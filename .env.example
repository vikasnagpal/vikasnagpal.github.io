# Supabase — leave unset to run in local fallback mode (seeded guestbook, localStorage only)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# "Grab some time" CTA destination (calendar link)
VITE_CALENDAR_URL=#
